import { Typography } from '@mui/material';

const AttendeeRegistrations = () => {
  return (
    <Typography variant="h4" align="center" gutterBottom>
      Attendee Registrations
    </Typography>
  );
};

export default AttendeeRegistrations;
